<!DOCTYPE html>
<html>
  <head>
    <title>Cepas 2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="preconnect" href="https://fonts.googleapis.com"> 
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> 
    
    <link rel="preconnect" href="https://fonts.googleapis.com"> 
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin> 
    <link href="https://fonts.googleapis.com/css2?family=Montserrat+Alternates:wght@100&family=Montserrat:ital,wght@0,200;0,500;0,700;1,200&display=swap" rel="stylesheet">

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <!-- link rel="stylesheet" href="https://unpkg.com/@picocss/pico@latest/css/pico.min.css" -->

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">

    <link rel="stylesheet" href="/assets/kaminari.css" as="style">
    <link rel="stylesheet" href="/assets/alineacion.css" as="style">

  </head>

  <body>
   
    <div class="container">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <nav class="navbar navbar-light btn" style="background-color: #e3f2fd; text-decoration: none; opacity:80%;">
        <div>
          <%= link_to "",  root_path, class: "fa fa-home fa-1x", title: "Inicio", style: "text-decoration: none;" %> |
          <%= link_to "Amigos", friends_path, style: "text-decoration: none;" %> |
          <%= link_to "Vinos", wines_path, style: "text-decoration: none;" %> |
          <%= link_to "Cepas", strains_path, style: "text-decoration: none;" %>
        </div>

        <div>
                <!-- BOTONERA DE LOGIN/LOGOUT -->
      <div class="align-start-box"  style="block-inline">
          <!-- % if current_friend % -->

        <% if friend_signed_in? %>
          <i class="fa fa-user-circle-o text-info sombra page-item page-link llink" style="block-inline" title="Usuario actual"><%= " #{current_friend.name} (#{current_friend.email}) " %></i>
          <!-- SALIR -->
          <%= link_to destroy_friend_session_path, data: { turbo_method: :delete }, class: "page-item", style: "text-decoration: none; display: inline-block" do %>
            <i class="fa fa-sign-out pagination pagination-box page-link llink" aria-hidden="true" title="Cerrar la sesión"></i>
          <% end %>
        <% else %>
          <!-- ENTRAR -->
          <%= link_to new_friend_session_path, class: "page-item", style: "text-decoration: none; display: inline-block" do %>
            <i class="fa fa-sign-in page-link  llink" aria-hidden="true" title="Iniciar sesión"></i>
          <% end %>
          <%= link_to "", new_friend_session_path,  class: "fa fa-sign-in fa-1x", title: "Inicia sesión" %>
          <%= link_to "", new_friend_registration_path,  class: "fa fa-user-plus fa-1x", title: "Registrarse" %>
        <% end %>
        <!-- FIN BOTONERA DE LOGIN/LOGOUT -->
      </div>

        </div>
      </nav>
      
      <%= yield %>

    </div>
   </body>
</html>
