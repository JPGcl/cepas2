<%= form_with(model: strain) do |form| %>
  <% if strain.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(strain.errors.count, "error") %> prohibited this strain from being saved:</h2>

      <ul>
        <% strain.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "Nombre de la Cepa", style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label "Disponible?", style: "display: block-inline" %>
    <%= form.check_box :available %>
  </div>

  <hr>
  
  <div>
    <%= form.submit "Guardar" %>
  </div>
<% end %>
