<%= form_with(model: wine) do |form| %>
  <% if wine.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(wine.errors.count, "error") %> prohibited this wine from being saved:</h2>

      <ul>
        <% wine.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "Nombre del vino", style: "display: block" %>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label "Año", style: "display: block" %>
    <%= form.text_field :year %>
  </div>
  <div>
    <%= form.label "Cepa", style: "display: block" %>
    <%= form.text_field :strain_id %>
  </div>
  <div>
    <%= form.label "Aprobado?", style: "display: block-inline" %>
    <%= form.check_box :approved %>
  </div>

  <hr>
  
  <div>
    <%= form.submit "Guardar" %>
  </div>
<% end %>
