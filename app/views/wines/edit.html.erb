<h1>Editando el vino</h1>
  <div class="shadow-lg p-3 mb-5 bg-gray rounded">
    <%= render "form", wine: @wine %>
    <hr>
    <div>
      <!-- % = link_to "Show this wine", @wine %  -->
      <%= link_to "Volver", wines_path %>
    </div>
  </div>
